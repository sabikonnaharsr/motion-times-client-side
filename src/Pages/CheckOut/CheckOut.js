import React from 'react';
import UseTitle from '../Hooks/UseTitle';

const CheckOut = () => {
    UseTitle('title')
    return (
        <div>
            <h1>checkout</h1>
        </div>
    );
};

export default CheckOut;